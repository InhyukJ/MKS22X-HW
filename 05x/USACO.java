public class USACO {
    public int bronze(String filename) {
	private int R, C, E, N;
	private int[][] lake;
	File file = new File(filename);
	Scanner in = new Scanner(file);
	while (in.hasNextLine()) { //parse all into an arraylist and separatethem
	    String word = in.next();
	    
    }
    
    public int silver(String filename);
}
